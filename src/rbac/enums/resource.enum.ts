export enum Resource {
  USER = 'user',
  PROPERTY = 'property',
  TRANSACTION = 'transaction',
  ROLE = 'role',
  PERMISSION = 'permission',
  AUDIT = 'audit',
  BLOCKCHAIN = 'blockchain',
  FILE = 'file',
  DOCUMENT = 'document',
  HEALTH = 'health',
  CONFIG = 'config',
}
